{
  "private": "true",
  "workspaces": [
    "lib",
    "server"
  ],
  "scripts": {
    "server:dev": "npm run dev --workspace=server",
    "server:ace": "npm run ace --workspace=server",
    "lib:build": "npm run build --workspace=lib"
  }
}
